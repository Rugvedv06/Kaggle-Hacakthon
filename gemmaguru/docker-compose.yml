version: '3.8'

services:
  db:
    image: ankane/pgvector
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: gemmaguru
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
